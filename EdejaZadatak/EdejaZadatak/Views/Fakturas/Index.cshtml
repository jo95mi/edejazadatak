@model IEnumerable<EdejaZadatak.Models.Faktura>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.DatumFakture)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Ukupno)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.DatumFakture)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Ukupno)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.BrojFakture }) |
            @Html.ActionLink("Details", "Details", new { id=item.BrojFakture }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.BrojFakture })
        </td>
    </tr>
}

</table>
